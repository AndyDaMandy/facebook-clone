<div id="<%= dom_id post %>">
  <p>
    <strong>User:</strong>
    <% @user = User.find(post.user_id)  %>
    <%= @user.profile.first_name %>
    <%= @user.profile.last_name %>
  </p>

  <p>
    <strong>Content:</strong>
    <%= post.content %>
  </p>

</div>
