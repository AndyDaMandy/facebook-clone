# frozen_string_literal: true

# Serves static pages with the potential to add more over time
class PagesController < ApplicationController
  # this holds your privacy policy so don't delete it!
  def privacy; end
end
